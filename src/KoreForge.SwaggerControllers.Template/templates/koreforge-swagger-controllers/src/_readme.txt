Per-API projects are generated here by scr/generate.ps1. Do not commit anything
under this folder until you have run the generator at least once.

The exception: src/MyCompany.External.Apis.AssemblyMarker/ is created by the
template itself and must remain.
